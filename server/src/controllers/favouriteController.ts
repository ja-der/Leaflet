import { Request, Response } from "express";
import axios from "axios";

// export const getFavourites = (req: Request, res: Response) => {
//     try {
//         const response = await axios.get()

//     } catch(error) {
//         res.status(500).json({error: "Error fetching user favourites."})
//     }
// }
